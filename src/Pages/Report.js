import React from 'react';
import { weatherData } from './weatherData';
const Report = () => {
  const data = weatherData(scale);
  return (
    <section>
      <h1>Your weather report in</h1>
    </section>
  );
};
export default Report;
